{
  "name": "k8s-3d-dashboard",
  "version": "1.0.0",
  "description": "3D visualization dashboard for Kubernetes clusters",
  "scripts": {
    "dev": "vite",
    "generate-demos": "node scripts/generate-demos.js",
    "build": "npm run generate-demos && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "3d-force-graph": "^1.80.0",
    "three": "^0.179.0"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
